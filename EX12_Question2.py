# Whats going on here, Can you explaine the output?
tup ='Hello'
print(len(tup))

tup ='Hello',
print(len(tup))

# The first command is asking for the length of the "string" in the variable tup. 5 letters
# The 2nd command has an additional comma at the end sugesting other items to follow.
# The length of the tuple is 1 as their is only one item in it.
